#include <bits/stdc++.h>
using namespace std;

int main() {
    // vector<int> v;
    // vector<int> v(4);
    // vector<int> v(4, 6);
    // vector<int> v2(v);
    int a[5]={2,7,4,9,4};
    // vector<int> v(a, a+5);
    vector<int> v={3,4,6};
    for (int i = 0; i < v.size(); i++)
    {
        cout << v[i] << " ";
    }
    
    return 0;
}