#include <bits/stdc++.h>
using namespace std;

int main() {
    list<int> myList={101, 82, 120, 32, 50};

    // // myList.remove(20);
    // myList.sort();
    // myList.sort(greater<int>());

    // myList.unique();

    for(int val:myList){
        cout << val << " ";
    }
    return 0;
}